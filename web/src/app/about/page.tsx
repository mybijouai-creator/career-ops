import { CreditsView } from "@/components/onboarding/credits-view";

export const metadata = { title: "Credits & story — career-ops" };

export default function AboutPage() {
  return <CreditsView />;
}
